# EIP-8205 landing page

This repository contains the static, non-normative landing page published at <https://eip8205.com/>. It has no build step or runtime application framework: Cloudflare serves the repository root as HTML, CSS, a small same-origin progressive-enhancement script, text, XML, and image assets. EVA Hub totals optionally refresh from a public API; a dated HTML snapshot keeps the page usable offline.

## Source hierarchy

The landing page explains the proposal; it does not define it. Check technical claims against the current [EIP](https://eips.ethereum.org/EIPS/eip-8205), the merged [consensus specification](https://github.com/ethereum/consensus-specs/tree/master/specs/_features/eip8205), and the [system-contract implementation](https://github.com/ethereum/sys-asm/pull/55), in that order. Stop and resolve material disagreements before updating public copy.

## Files

- `index.html` contains semantic page content, metadata, the authoritative `WebSite`/`TechArticle`/`FAQPage` structured-data graph, and one deferred reference to `main.js`. It contains no visual style declarations, inline event handlers, or inline executable scripts.
- `styles.css` is the single visual source of truth. It is organized as tokens, base rules, layout primitives, page components, interaction states, motion, and responsive/accessibility overrides.
- `main.js` refreshes EVA Hub totals and progressively enhances mobile navigation, the desktop attack sequence, fine-pointer artwork tilt, and FAQ disclosure motion. At widths up to 780px, navigation uses a labelled toggle with an expanded state and closes on link selection, Escape, outside click, or focus leaving the menu. Desktop navigation remains inline; without JavaScript, the same links stay visible. All content and native disclosure controls remain usable without JavaScript, and the enhancement observes reduced-motion and pointer-capability preferences.
- Native HTML provides the interaction model: FAQ disclosure uses `details` and `summary`, repeated processes use lists, and all content works without JavaScript. Markerless native lists deliberately retain `role="list"` so Safari and VoiceOver do not drop their list semantics.
- `robots.txt`, `sitemap.xml`, and `llms.txt` provide crawler and machine-readable entry points.
- `_headers` defines source-controlled security headers for Cloudflare static assets, including a CSP that permits only same-origin external scripts and styles while forbidding unsafe inline or evaluated code.
- `.assetsignore` defines the intended non-public files when Cloudflare deploys this root through Workers Static Assets. The source-integrity check classifies every top-level entry as public or excluded; release verification must still confirm that the owner-controlled Cloudflare build actually uses this assets directory and ignore file.
- `assets/social/` contains the current social card set; the default Open Graph card is 1200×630.
- `assets/fonts/` contains the versioned Latin WOFF2 files used by the page and their upstream SIL Open Font License texts; the page makes no runtime font request to a third party.
- `scripts/qa.mjs` is a dependency-free source-integrity check used locally and in GitHub Actions. It checks repository invariants; it is not a browser, layout, accessibility, or production-deployment test.
- `scripts/eva-support.test.mjs` checks the totals, stake-weighted denominator, rounding, response validation, and snapshot preservation on API failures without making network requests.

## Local QA

Open `index.html` directly in a browser for a local preview; no server or build is required. Keep the file alongside `styles.css`, `main.js`, and `assets/`. Local resources use document-relative paths so the same files work from disk and from the root of the canonical domain. The shared desktop and mobile navigation includes a Supporters link to `#supporters`, the start of the full section with quotations and Hegotá rankings; without JavaScript, narrow screens show the brand and support action above the four section links.

Run `make serve`, then open <http://127.0.0.1:4173/>. In another terminal run `make check`. The check validates JavaScript syntax, sitemap XML parsing, the publish surface, local references, JSON-LD structure, essential document semantics, CSP boundaries, optimized assets, font licenses, and known historical proposal values. It deliberately does not claim to validate rendering or production behavior. Before release, test keyboard-only navigation, animations and reduced motion, 200% and 400% zoom, and representative phone, tablet, and desktop widths.

## Adding public support

The `#supporters` section in `index.html` contains EVA Hub signal totals, public quotations, and a separate `#rankings` group titled “EIP-8205 in Hegotá rankings”. This is a selection of rankings, not a comprehensive survey of client priorities or a set of formal votes. Selected A, B, and C grades appear together in the same list with their original sources. Quotations are grouped in reading order as staking protocols (`#supporters-protocols`), node operators (`#supporters-operators`), auditors (`#supporters-auditors`), and researchers (`#supporters-research`). Add a quotation as a new `<li>` to the relevant group's `.supporters__list` using the existing `.supporter` figure and a stable `supporter-…` ID. Each quote places the author beside the text on wide screens and above it on narrow screens. Add rankings to `.rankings__list` using `.ranking`; its tinted background, compact attribution, and prominent grade distinguish it from a quote. The groups and lists have no fixed entry count. Keep the content in HTML so it stays readable from disk and without JavaScript.

The `#validator-support` panel makes one anonymous GET to [EVA Hub's EIP-8205 endpoint](https://api.ethva.net/eips/8205) per page load, with a six-second timeout and no cookies or referrer. This endpoint is used by the public Hub client; a documented external API contract was not found. The API returned HTTP 200 without authentication on 2026-09-15, and CORS allowed both the canonical site origin and the `null` origin used by files opened from disk. The CSP permits connections only to this endpoint. Failures and malformed data preserve the dated HTML snapshot; an explicitly unapproved proposal hides the panel. There is no polling or dependency on a local server.

Balances are supplied in wei. Support is `yesVoteBalance / (yesVoteBalance + noVoteBalance + abstainVoteBalance)`; BigInt arithmetic preserves the weights before rounding the percentage to one decimal place. The count sums the three signal counts and must not be described as a count of validator keys or a percentage of all Ethereum stake. The [methodology](https://hub.ethva.net/methodology) describes this as non-binding sentiment among participating operators. Keep all three choices in the breakdown, distinguish zero participation from opposition, and avoid rounding partial support to an unqualified 100%. The check timestamp records when data was retrieved, never the EIP's `createdAt`. When refreshing the offline snapshot, update all six `data-eva-*` fields together. The initial snapshot was retrieved on 2026-09-15 at 19:00 UTC: 7 yes / 48,620 ETH, 0 no / 0 ETH, 1 abstain / 544 ETH; 8 signals and 49,164 ETH in total.

For a quotation, choose a contiguous excerpt that gives a concrete reason for support. Each entry should add a distinct reason to the section. Prefer complete sentences, including a short concluding sentence when it completes the argument. Preserve its words, punctuation, capitalization, and spelling exactly; do not rewrite, join separate passages, or silently shorten it. Put the source URL in both `blockquote[cite]` and the visible source link. Include the speaker or the specific team speaking, a verified professional role, and the publication date. Use editorial context only when needed to understand the excerpt accurately, and keep it outside the blockquote. Do not turn a personal comment into an organization’s endorsement or equate different authors’ tier definitions.

Link the author’s name to their verified website or public profile with `.supporter__profile`. The source link below the quote opens the original statement. Keep visible `#` permalinks on section headings only, without adding them beside individual authors or ranking entries. Author links and section permalinks stay free of underlines on hover.

The MixBytes entry uses the complete implementation paragraph from [its forum comment](https://ethereum-magicians.org/t/eip-8205-withdrawal-credentials-preregistration/28084/6), published and checked on 2026-09-15. The post is by `plmorozov`, explicitly speaking for the MixBytes team. The paragraph highlights the deposit-time check, reuse of the EIP-7002/7251 request pattern, and compatibility with solo staking, top-ups, and existing validators. Preserve all three sentences exactly as supplied and verified against the original. The full comment also discusses integration requirements and caveats, so the source link remains part of the entry.

The protocol entries were checked on 2026-09-16 against the public forum posts. [ether.fi's comment](https://ethereum-magicians.org/t/eip-8205-withdrawal-credentials-preregistration/28084/7), by `seongyun-ko` on 2026-09-15, explicitly speaks for the team; its quotation is the complete sentence about binding the validator pubkey to the intended withdrawal credentials before funding. Keep the source link for the full integration context, including the need for an atomic proof of an active, unexpired preregistration. [Kelp's comment](https://ethereum-magicians.org/t/eip-8205-withdrawal-credentials-preregistration/28084/8), by `mihailomaksa` on 2026-09-16, explicitly adds Kelp's support; its complete quoted sentence highlights how a standard credential check before depositing would simplify its design. Preserve both sentences' original wording and punctuation.

Lido is currently omitted from the supporters list pending a dedicated public post. [George's earlier forum proposal](https://research.lido.fi/t/eip-8205-towards-safe-delegated-staking-deposits/11357/1) remains background material; use the forthcoming post as the quotation source when adding Lido again. The logo is retained at `drafts/supporters/lido.svg`, outside the published assets; `drafts/` is excluded by `.assetsignore`. Move it back to `assets/supporters/` when adding the new entry.

FP Validated's quotation and primary source link come from [the team's Ethereum Magicians comment](https://ethereum-magicians.org/t/eip-8205-withdrawal-credentials-preregistration/28084/9), posted by `JayLovesPotato` on 2026-09-16. The quotation is the unchanged main clause about a common security foundation for delegated staking while preserving the flow for validators that do not require preregistration; only the source sentence's introductory clause is omitted. Its separate “Also” row keeps the earlier Lido forum comment and [Four Pillars' April 2026 research](https://research.4pillars.io/en/research/monthly-eip---apr-2026-ft-staked-assets-at-work-the-prepared-win) as additional reading. The research covers an earlier proposal revision and must not be used for current request fields or consensus parameters.

A ranking uses `.ranking` without a blockquote. Name its source and recorded date. Teku’s A grade is explicitly labelled as listed on [Forkcast](https://forkcast.org/upgrade/hegota/client-priority/), whose public data was rechecked on 2026-09-15 and dates that entry to 2026-09-10. The aggregator does not link an original team statement, so do not describe it as a verified official team endorsement. A separate `.ranking__individual` row records Paul Harris’s personal A grade from [his X post](https://x.com/rolfyone/status/2097956563994714481), published on 2026-09-10 and checked on 2026-09-15. He explicitly distinguishes his own views from the Teku team’s position; his post is the source for his individual row only. Use `#supporter-teku` for the Teku entry. Update the page’s visible last-updated date, `dateModified`, and sitemap date when adding entries, then run `make check` and inspect the layout at narrow and wide widths.

L2BEAT’s B grade at `#ranking-l2beat` comes from [its research team’s Hegotá ranking](https://forum.l2beat.com/t/l2beats-research-team-hegota-ranking/439), published on 2026-09-09. Keep the grade as B and attribute it specifically to the research team. Its explanation also appears verbatim in the quotations list at `#supporter-l2beat`.

Prysm's C grade at `#ranking-prysm` comes from [its Hegotá assessment](https://hackmd.io/@ttsao/prysm-view-hegota), checked on 2026-09-16. The source says its C-tier proposals need stronger arguments before it would recommend inclusion, and does not currently consider the case for prioritizing EIP-8205 strong enough. Display the C grade in the same `#rankings` list as Teku and L2BEAT, with a link to the assessment and no editorial explanation below the grade. The displayed 2026-09-15 date is the source document's own date: its description says it was last updated then, and its metadata records creation at 17:08 UTC and the update at 17:13 UTC on that day. Forkcast independently records the same update date and links the primary source.

The author images in `assets/supporters/` are local copies of public profile images or official brand marks, used only to identify the attributed speaker. Keep intrinsic dimensions, empty `alt` text when the adjacent name identifies the author, and lazy loading. Source provenance was checked on 2026-09-14 unless noted otherwise:

| Asset | Source and scope |
| --- | --- |
| `tomer-ganor.webp` | Portrait from [Tomer Ganor’s Certora author page](https://www.certora.com/author/tomer-ganor), served at 192×192 by Certora’s image CDN. The [EthCC speaker description](https://ethcc.io/ethcc-9/agenda/designing-defi-resilience-inside-aave-v4-security-blueprint) documents his security-research role. His quoted forum comment is personal. |
| `l2beat.png` | Official 180×180 touch icon linked by the [L2BEAT forum](https://forum.l2beat.com/). The quoted ranking expressly represents its research team. |
| `mixbytes.png` | Unmodified 200×200 organization avatar from [MixBytes’ official GitHub profile](https://github.com/mixbytes), linked by [its website](https://mixbytes.io/); checked on 2026-09-15. The quoted forum post explicitly represents the team. |
| `fp-validated.png` | Public avatar from [FP_Validated’s Lido governance profile](https://research.lido.fi/u/FP_Validated), which identifies the team as Four Pillars’ validator arm. |
| `etherfi.png` | Unmodified 96×96 light-theme icon linked by [ether.fi's official website](https://www.ether.fi/), from [its favicon asset](https://www.ether.fi/images/favicon/favicon-light-96x96.png); checked on 2026-09-16. |
| `kelp.svg` | Unmodified 79×32 Kelp wordmark embedded as an SVG image in [Kelp's official website](https://kerneldao.com/kelp/); checked on 2026-09-16. |
| `drafts/supporters/lido.svg` | Unmodified SVG icon with a 32×32 viewBox from [Lido's official favicon](https://lido.fi/favicon.svg), linked by [its website](https://lido.fi/); checked on 2026-09-16. Retained outside the published assets for a future entry. |
| `paul-harris.jpg` | Unmodified 200×200 public avatar from [@rolfyone on X](https://x.com/rolfyone), checked on 2026-09-15. His [GitHub profile](https://github.com/rolfyone) identifies him as Paul Harris; his X profile and Teku contributions identify his developer role. |
| `teku.svg` | Unmodified logo from the [official Teku documentation](https://docs.teku.consensys.io/img/logo.svg), used for the client’s entry in the rankings group. |
| `prysm.svg` | Unmodified 153×173 SVG icon from the [official Prysm documentation](https://prysm.offchainlabs.com/docs/images/Prysm.svg), linked by [its documentation home page](https://prysm.offchainlabs.com/); checked on 2026-09-16. |

## Release model

Cloudflare Workers Builds deploys the root of `main` to the canonical apex domain. A page change is not live until it is committed and pushed in this repository; updating a parent-repository submodule pointer does not publish it. Treat every push to `main` as a production release, verify the Cloudflare build SHA, inspect the canonical URL, and keep the prior good commit available for rollback.

Repository owners should protect `main`, require review and the check emitted by `.github/workflows/qa.yml`, and prevent direct pushes. After its first run, select the exact check context shown by GitHub in branch-protection settings. Cloudflare should redirect every HTTP and `www` request to the canonical HTTPS apex, keep preview deployments out of search indexes, and expose a documented rollback path. These account-level settings cannot be enforced from this repository.

## External release checks

The repository cannot verify Cloudflare account security, DNS, real-device assistive technology, search-engine recrawl state, social-network caches, or the legal provenance of existing images. Before a broad campaign, an owner should confirm asset rights, run the major social-card debuggers, submit the sitemap in Google Search Console and Bing Webmaster Tools, and manually exercise VoiceOver or NVDA plus iOS Safari and Android Chrome. The current social card set is technically complete; no additional illustration is required for this release.
